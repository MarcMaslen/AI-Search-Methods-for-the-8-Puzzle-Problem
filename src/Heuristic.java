package ApplicationProgramming;

//-----------------------------------------
//
// This defines the heuristic  so we can call it
// This is sorta pointless but is the way I have chosen to do it
//
//------------------------------------------
public enum Heuristic {
    H_ONE,
}